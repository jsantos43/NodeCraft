{
  "post": {
    "summary": "forgot User password",
    "description": "Starts the password-reset flow: generates a temporary reset token and emails it to the user. Public route (no authentication). Requires the email service to be available (503 otherwise).",
    "tags": [
      "Auth"
    ],
    "responses": {
      "200": {
        "$ref": "../../components/responses/user.json#/default"
      },
      "404": {
        "$ref": "../../components/responses/error.json#/NotFound"
      },
      "500": {
        "$ref": "../../components/responses/error.json#/Internal"
      },
      "503": {
        "$ref": "../../components/responses/error.json#/UnavailableService"
      }
    },
    "requestBody": {
      "$ref": "../../components/requestBody/auth.json#/forgot"
    }
  }
}
