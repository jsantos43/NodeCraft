{
  "post": {
    "summary": "login User",
    "description": "Authenticates the user with email and password. On success sets the `accessToken` (JWT, 15min) and `refreshToken` (3 days) as httpOnly cookies and returns the user. Public route.",
    "tags": [
      "Auth"
    ],
    "responses": {
      "200": {
        "$ref": "../../components/responses/user.json#/default"
      },
      "401": {
        "$ref": "../../components/responses/auth.json#/invalidLogin"
      },
      "500": {
        "$ref": "../../components/responses/error.json#/Internal"
      }
    },
    "requestBody": {
      "$ref": "../../components/requestBody/auth.json#/login"
    }
  }
}
