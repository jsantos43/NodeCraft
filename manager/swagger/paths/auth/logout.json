{
  "post": {
    "summary": "logout User",
    "description": "Logs the current user out: wipes the stored refresh token and clears the `accessToken`/`refreshToken` cookies. Requires a valid access token.",
    "tags": [
      "Auth"
    ],
    "responses": {
      "200": {
        "$ref": "../../components/responses/user.json#/default"
      },
      "401": {
        "$ref": "../../components/responses/error.json#/Unathorized"
      },
      "500": {
        "$ref": "../../components/responses/error.json#/Internal"
      }
    }
  }
}
